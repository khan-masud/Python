# Check that a tuple type cannot be changed in python.

tuples = ("Maaud", 10, 93)

tuples[1] = "The Offline Dreamer" # This line will return error because tuple is immutable so that we can not change any element in tuples.